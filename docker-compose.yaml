services:
  service-a:
    build: ./service-a
    ports:
      - "5000:5000"
    environment:
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
      - OTEL_SERVICE_NAME=service-a
      - OTEL_TRACES_EXPORTER=otlp
      - OTEL_METRICS_EXPORTER=otlp
    depends_on:
      - service-b
      - otel-collector
    networks:
      - otel-net
    extra_hosts:
      - "nginx-gateway:10.1.1.5"

  service-b:
    build: ./service-b
    ports:
      - "5001:5001"
    environment:
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
      - OTEL_SERVICE_NAME=service-b
      - OTEL_TRACES_EXPORTER=otlp
      - OTEL_METRICS_EXPORTER=otlp
    depends_on:
      - otel-collector
    networks:
      - otel-net

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.109.0
    command: [--config=/etc/otel-collector-config.yml]
    volumes:
      - ./otel-collector-config.yml:/etc/otel-collector-config.yml
    ports:
      - "4317:4317"  # OTLP HTTP
      - "4318:4318"  # OTLP gRPC
      - "8888:8888"  # Prometheus metrics (optional)
    depends_on:
      - jaeger
    networks:
      - otel-net

  jaeger:
    image: jaegertracing/all-in-one:latest
    ports:
      - "16686:16686"  # Jaeger UI
      - "14268:14268"  # Jaeger HTTP collector (optional)
      - "14317:4317"    # OTLP gRPC (for collector export)
      - "14318:4318"    # OTLP HTTP (optional)
    networks:
      - otel-net

networks:
  otel-net:
    driver: bridge
